<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view"
    >

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_overall"
            android:icon="@drawable/ic_nav_overall"
            android:title="@string/nav_overall" />
        <item
            android:id="@+id/nav_random"
            android:icon="@drawable/ic_nav_random"
            android:title="@string/nav_random" />
        <item
            android:visible="false"
            android:id="@+id/nav_future_read"
            android:icon="@drawable/ic_nav_future_read"
            android:title="@string/nav_future_read" />
        <item
            android:visible="false"
            android:id="@+id/nav_favorite"
            android:icon="@drawable/ic_nav_favorite"
            android:title="@string/nav_favorite" />
        <item
            android:id="@+id/nav_by_tags"
            android:icon="@drawable/ic_nav_by_tags"
            android:title="@string/nav_by_tags" />
        <item
            android:visible="false"
            android:id="@+id/nav_by_date"
            android:icon="@drawable/ic_nav_by_date"
            android:title="@string/nav_by_date" />
        <item
            android:visible="true"
            android:id="@+id/nav_search"
            android:icon="@drawable/ic_nav_search"
            android:title="@string/nav_search" />

    </group>

    <item android:title="Communicate"
        android:visible="false">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>
